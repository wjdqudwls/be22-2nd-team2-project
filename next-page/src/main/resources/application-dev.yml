spring:
  config:
    activate:
      on-profile: dev

  jpa:
    hibernate:
      ddl-auto: validate  # 운영에 가까운 환경, 스키마 검증만
    show-sql: false
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    com.team2.nextpage: INFO
    org.springframework.security: INFO
